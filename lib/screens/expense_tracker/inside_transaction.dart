import 'package:flutter/material.dart';

class InsideTransaction extends StatelessWidget {
  const InsideTransaction({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}